<div class="page-header">
  <%= link_to '<button class="nav nav-pills pull-right btn btn-primary" style="margin-left:10px;">Editer le type</button>'.html_safe, edit_ticket_type_path(@ticket_type) %>
  <%= link_to '<button class="nav nav-pills pull-right btn btn-primary">Liste des types</button>'.html_safe, ticket_types_path %>
  <h1>Type <em><%= @ticket_type.name %></em></h1>
</div>

<p id="notice"><%= notice %></p>

<div class="well">
  <table class="table table-striped">
    <tr>
      <th width="20%">Name</th>
      <td width="80%"><%= @ticket_type.name %></td>
    </tr>
  </table>
</div>
